#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../minify/bin/minify" "$@"
  ret=$?
else 
  node  "$basedir/../minify/bin/minify" "$@"
  ret=$?
fi
exit $ret
